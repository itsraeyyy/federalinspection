'use client';

import React, { Suspense } from 'react';
import { useSearchParams } from 'next/navigation';
import PublicNeedFormPage from '../submit/page';

export default function FlagotPublicAliasPage() {
  return <PublicNeedFormPage />;
}
